function rectangle(){
    let rectangleLength = Number(prompt("Enter the Length for Rectangle: "));
    let rectangleWidth = Number(prompt("Enter the Width for Rectangle: "));

    let rectangleArea = rectangleLength * rectangleWidth;

    alert(`Area of Rectangle: ${rectangleArea}`);
}